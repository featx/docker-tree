---

- name: Register logrotate configurations
  file:
    src:  '{{ item }}'
    dest: '/etc/logrotate/{{ item | basename }}'
    state: link
    force: yes
  with_fileglob:
    - /usr/local/etc/logrotate/*
